name: Emergency Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    name: Validate and deploy selected main revision
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/cicd.yml
    secrets: inherit
